scan_filter_chain:
- name: angle1
  type: laser_filters/LaserScanAngularBoundsFilterInPlace
  params:
    lower_angle: 2
    upper_angle: 4.28
- name: box_filter
  type: laser_filters/LaserScanRangeFilter
  params:
    use_message_range_limits: false   # if not specified defaults to false
    lower_threshold: 0.2             # if not specified defaults to 0.0
    upper_threshold: 12.0             # if not specified defaults to 100000.0
    lower_replacement_value: 0    # if not specified defaults to NaN
    upper_replacement_value: 0     # if not specified defaults
  

